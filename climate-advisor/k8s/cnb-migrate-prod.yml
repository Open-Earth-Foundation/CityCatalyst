# CNB migration job for the production database.
apiVersion: batch/v1
kind: Job
metadata:
  generateName: climate-advisor-cnb-migrate-prod-
  namespace: default
spec:
  backoffLimit: 0
  ttlSecondsAfterFinished: 86400
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: climate-advisor-cnb-migrate
          image: ghcr.io/open-earth-foundation/citycatalyst-climate-advisor:stable
          imagePullPolicy: Always
          envFrom:
            - configMapRef:
                name: climate-advisor-db-configmap-prod
          command: ["alembic", "-c", "cnb-alembic.ini", "upgrade", "head"]
