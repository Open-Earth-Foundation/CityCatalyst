apiVersion: apps/v1
kind: Deployment
metadata:
  name: hiap-prod
  namespace: default
  labels:
    app: hiap-prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hiap-prod
  strategy:
    type: Recreate # needed because of high CPU request. Rolling update will not find a second large instance
  template:
    metadata:
      labels:
        app: hiap-prod
    spec:
      containers:
        - name: hiap-prod
          image: ghcr.io/open-earth-foundation/citycatalyst-hiap:stable
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
              name: http
          env:
            - name: API_HOST
              value: "0.0.0.0"
            - name: API_PORT
              value: "8000"
          resources:
            requests:
              memory: "1Gi"
              cpu: "500m"
            limits:
              memory: "2Gi"
              cpu: "1000m"
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
          startupProbe:
            httpGet:
              path: /
              port: http
            periodSeconds: 10
            failureThreshold: 30

