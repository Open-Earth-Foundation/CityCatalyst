# Climate Advisor Service configuration
# Only LLM secrets belong in env. Non-secret routing, model, timeout, retry,
# and prompt settings live in llm_config.yaml.
OPENROUTER_API_KEY=sk-or-placeholder
OPENAI_API_KEY=sk-or-placeholder
FIRECRAWL_API_KEY=fc-placeholder
LANGSMITH_API_KEY=lsv2-or-placeholder
CA_CORS_ORIGINS=*
CA_FEATURE_FLAGS=

# MLflow observability
MLFLOW_ENABLED=true
MLFLOW_TRACKING_URI=https://mlflow-dev.openearth.dev
MLFLOW_ENVIRONMENT=dev
MLFLOW_EXPERIMENT_NAME=Clima
MLFLOW_HTTP_REQUEST_TIMEOUT=3
MLFLOW_HTTP_REQUEST_MAX_RETRIES=1
MLFLOW_HTTP_REQUEST_BACKOFF_FACTOR=1
MLFLOW_HTTP_REQUEST_BACKOFF_JITTER=0
GIT_PYTHON_REFRESH=quiet
MLFLOW_ASYNC_LOGGING_ENABLED=true

# Postgres persistence configuration
# Default host-facing CA DB URL. Use localhost:5433 to avoid conflict with
# local CityCatalyst Postgres typically running on localhost:5432.
CA_DATABASE_URL=postgresql://climateadvisor:climateadvisor@localhost:5433/climateadvisor
CA_DATABASE_POOL_SIZE=5
CA_DATABASE_MAX_OVERFLOW=10
CA_DATABASE_POOL_TIMEOUT=30
CA_DATABASE_ECHO=false

# CityCatalyst Integration
CC_API_KEY=your-cc-service-api-key-here
CC_BASE_URL=http://localhost:3000
# Complete inline JSON body limit for optional CC-produced Markdown ingest.
# This is independent from CC's source-PDF upload limit.
CNB_MARKDOWN_REQUEST_MAX_BYTES=20971520
