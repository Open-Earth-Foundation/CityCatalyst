apiVersion: v1
kind: ConfigMap
metadata:
  name: cc-test-db-configmap
  namespace: default
data:
  DATABASE_HOST: "cc-db"
  DATABASE_NAME: "cctest"
  DATABASE_USER: "cctest"
  DATABASE_PASSWORD: "cctest"
