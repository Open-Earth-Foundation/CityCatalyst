apiVersion: v1
kind: Service
metadata:
  name: climate-advisor-service-test
  namespace: default
  labels:
    app: climate-advisor-test
spec:
  selector:
    app: climate-advisor-test
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      name: http
  type: ClusterIP
